import static org.junit.Assert.*;
import org.junit.Test;

import dao.TestDBAccess;
import domain.ClipboardManipulator;
import domain.TestClipboardAccess;

public class ClipboardManipulatorTest {
	
	@Test
	public void updateClipboardUpdatesClipboard() {
		//String[] noStrings = {""};
		//TestClipboardAccess clipboardAccess = new TestClipboardAccess(noStrings, true);
		//TestDBAccess dao = new TestDBAccess("test");
		//ClipboardManipulator clipboardManipulator = new ClipboardManipulator(clipboardAccess, dao, noStrings);
		//clipboardManipulator.updateClipboard();
		//assertTrue("updateClipboardUpdatesClipboard", clipboardAccess.getWritten().get(0).equals("test"));
	}
}
